[package]
name = "httpc-requester"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
readme = "README-en.md"

[lib]
crate-type = ["cdylib"]
name = "httpc"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = "3"
